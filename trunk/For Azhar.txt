line 18 & 23 Change "from" to "to"

Rename functions 

SyncPlanWriteLog(), SyncPlanLogEntry() to WritePreviewLog(), WritePreviewLogEntry()

SyncBeginLogEntry() to WriteLogHeader()

SyncResultsLogEntry() to WriteSyncResultsLogEntry()

Inside WriteLog() function

Refactor the switch case at line 343: 

//....
 oriPath = srcPath;
 oriSize = srcSize;
 destPath = tgtPath;
 destSize = tgtSize;

Place the codes before the switch case, inside your if statement?

Rename CopyRenameLogEntry(), CreateLogEntry(), DeleteLogEntry();
Overload with one function name (WriteLogEntry())





